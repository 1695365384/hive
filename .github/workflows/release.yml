name: Release Desktop App

on:
  push:
    tags:
      - 'v*'

env:
  NODE_VERSION: '20'

jobs:
  # ============================================
  # Build server + frontend once (shared)
  # ============================================
  build-common:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: '9'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Build server
        run: pnpm --filter @bundy-lmw/hive-server build

      - name: Build frontend
        run: pnpm --filter @bundy-lmw/hive-desktop build

      - name: Upload server artifact
        uses: actions/upload-artifact@v4
        with:
          name: server-dist
          path: apps/server/dist
          retention-days: 1

      - name: Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: apps/desktop/dist
          retention-days: 1

  # ============================================
  # Build Tauri on Windows (MSI)
  # ============================================
  build-windows:
    needs: build-common
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: '9'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          rustup default stable

      - name: Download server
        uses: actions/download-artifact@v4
        with:
          name: server-dist
          path: apps/server/dist

      - name: Download frontend
        uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: apps/desktop/dist

      - name: Build Tauri (MSI)
        working-directory: ./apps/desktop
        run: npx @tauri-apps/cli build --bundles msi

      - name: Find MSI
        id: msi
        shell: pwsh
        run: |
          $f = Get-ChildItem "apps/desktop/src-tauri/target/release/bundle/msi" -Filter "*.msi" | Select-Object -First 1
          echo "path=$($f.FullName)" | Out-String -Width 4096

      - name: Upload MSI
        uses: actions/upload-artifact@v4
        with:
          name: hive-desktop-windows-x64
          path: ${{ steps.msi.outputs.path }}
          retention-days: 1

  # ============================================
  # Build Tauri on macOS (ARM64 + x64)
  # ============================================
  build-macos:
    needs: build-common
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [arm64, x64]

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: '9'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install Rust
        run: |
          rustup default stable
          rustup target add ${{ matrix.arch == 'arm64' && 'aarch64-apple-darwin' || 'x86_64-apple-darwin' }}

      - name: Download server
        uses: actions/download-artifact@v4
        with:
          name: server-dist
          path: apps/server/dist

      - name: Download frontend
        uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: apps/desktop/dist

      - name: Build Tauri (${{ matrix.arch }})
        working-directory: ./apps/desktop
        run: npx @tauri-apps/cli build --target ${{ matrix.arch == 'arm64' && 'aarch64-apple-darwin' || 'x86_64-apple-darwin' }} --bundles dmg

      - name: Find DMG
        id: dmg
        shell: bash
        run: |
          find apps/desktop/src-tauri/target -name "*.dmg" -type f | head -1 | xargs -I{} echo "path={}"

      - name: Upload DMG
        uses: actions/upload-artifact@v4
        with:
          name: hive-desktop-macos-${{ matrix.arch }}
          path: ${{ steps.dmg.outputs.path }}
          retention-days: 1

  # ============================================
  # Create GitHub Release
  # ============================================
  release:
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract version
        id: version
        shell: bash
        run: |
          VERSION="${GITHUB_REF#refs/tags/v}"
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
        env:
          GITHUB_REF: ${{ github.ref }}

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List artifacts
        shell: bash
        run: ls -la artifacts/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.VERSION }}
          name: Hive Desktop v${{ steps.version.outputs.VERSION }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            artifacts/*.msi
            artifacts/*.dmg
