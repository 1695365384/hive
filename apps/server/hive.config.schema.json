{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Hive Server Configuration",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference"
    },
    "server": {
      "type": "object",
      "properties": {
        "port": {
          "type": "integer",
          "default": 3000,
          "description": "HTTP server port"
        },
        "host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Host to bind to"
        },
        "logLevel": {
          "type": "string",
          "enum": ["debug", "info", "warn", "error"],
          "default": "info",
          "description": "Log level"
        }
      },
      "additionalProperties": false
    },
    "provider": {
      "type": "object",
      "required": ["id"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Provider ID (e.g., glm, anthropic, openai, deepseek)"
        },
        "apiKey": {
          "type": "string",
          "description": "API key (supports ${ENV_VAR} interpolation)"
        },
        "model": {
          "type": "string",
          "description": "Model to use"
        },
        "baseUrl": {
          "type": "string",
          "description": "API base URL"
        }
      },
      "additionalProperties": false
    },
    "plugins": {
      "type": "object",
      "description": "Plugin configurations. Key is plugin package name, value is passed as api.config",
      "additionalProperties": {
        "type": "object",
        "description": "Plugin-specific configuration (passed directly to plugin as api.config)",
        "additionalProperties": true
      }
    },
    "heartbeat": {
      "type": "object",
      "description": "Heartbeat schedule configuration",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable heartbeat scheduling"
        },
        "intervalMs": {
          "type": "integer",
          "default": 300000,
          "description": "Heartbeat check interval in milliseconds (default: 5 minutes)"
        },
        "model": {
          "type": "string",
          "description": "Override model for heartbeat checks"
        },
        "prompt": {
          "type": "string",
          "description": "Custom heartbeat prompt (default: reads HEARTBEAT.md)"
        }
      },
      "additionalProperties": false
    }
  }
}
