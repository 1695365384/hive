{
  "name": "aiclaw-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @aiclaw/core test",
    "test:e2e": "pnpm --filter @aiclaw/core test:e2e",
    "dev:app": "pnpm --filter @aiclaw/app dev",
    "cli": "pnpm --filter @aiclaw/core cli"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
